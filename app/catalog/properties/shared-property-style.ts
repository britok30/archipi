import React from "react";

const tableStyle: React.CSSProperties = {width: '100%', borderSpacing: '2px 0', marginBottom: '2px'};
const firstTdStyle: React.CSSProperties = {width: '6em', textTransform:'capitalize'};

const containerStyle: React.CSSProperties = {};

const PropertyStyle = {
  tableStyle,
  firstTdStyle,
  containerStyle,
};

export default PropertyStyle;
